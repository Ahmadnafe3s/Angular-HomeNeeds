import { ToggleWidthDirective } from './toggle-width.directive';

describe('ToggleWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleWidthDirective();
    expect(directive).toBeTruthy();
  });
});
